{% extends 'kiwicore.base.html.twig' %}

{% block breadcrumbs %}
    {{ page_breadcrumbs|raw }}
{% endblock %}

{% block page_title %}
    <h1>{{ page_title }}</h1>
{% endblock %}

{% block body %}
    <div class="container">
        <div class="col-2">
            <div class="card">
                <div class="card-title">
                    Renseignez les champs suivants :
                </div>
                <div class="card-body">
                    {{ form(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}